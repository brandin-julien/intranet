{% extends 'base.html.twig' %}

{% block body %}
    <h1>Matter</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ matter.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('matter_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('matter_edit', { 'id': matter.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
